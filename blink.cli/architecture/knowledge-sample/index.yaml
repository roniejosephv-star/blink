# tinkr-kb index — top-level knowledge base metadata.
# This file is auto-generated by the curation pipeline; humans don't edit it directly.
# It is the entry point for KB consumers (Tinkr core, the agent, third-party tools).

version: 2026.08.12          # KB version, year.month.day
kb_format: 1                 # Schema format version
signed_by: "ronie@tinkr.build" # GPG key fingerprint
signature: "gpg-signature-here"  # Generated by the build pipeline

# Counts per category, for the UI to show.
counts:
  facts: 2                   # In this sample; ~1000 in v1.0
  errors: 2                  # In this sample; ~200 in v1.0
  patterns: 1                # In this sample; ~50 in v1.0
  recipes: 1                 # In this sample; ~50 in v1.0
  stories: 0                 # In this sample; ~100 in v1.0

# Search index hints (for the Tinkr agent's retrieval).
# These are example tags the agent can match against; the real index
# is built from the full text + embeddings.
tags:
  - i2c
  - pull-ups
  - esp32
  - esp32s3
  - flash
  - micropython
  - firmware
  - i2c-driver
  - pattern
  - recipe

# The full list of entries. The agent uses this for browsing; the
# search engine uses it for the inverted index.
entries:
  facts:
    - id: fact/esp32-s3-i2c-pullups
      path: facts/esp32-s3-i2c-pullups.yaml
      summary: "ESP32 I2C pins have no internal pull-up resistors"
      chips: [esp32, esp32s2, esp32s3, esp32c3, esp32c6, esp32c2]
      verified_at: "2026-08-12"
    - id: fact/esp32-flash-addresses
      path: facts/esp32-flash-addresses.yaml
      summary: "ESP32 flash start addresses differ by chip family"
      chips: [esp32, esp32s2, esp32s3, esp32c3, esp32c6, esp32c2]
      verified_at: "2026-08-12"
  errors:
    - id: error/i2c-timeout
      path: errors/i2c-timeout.yaml
      summary: "I2C scan or read times out (ETIMEDOUT, errno 110)"
      category: i2c
    - id: error/flash-failed-wrong-address
      path: errors/flash-failed-wrong-address.yaml
      summary: "Firmware flash fails or bricks the device due to wrong flash start address"
      category: flash
  patterns:
    - id: pattern/micropython-i2c-driver
      path: patterns/micropython-i2c-driver.yaml
      summary: "The standard way to write an I2C device driver in MicroPython"
  recipes:
    - id: recipe/esp32-tinkr-led-deploy
      path: recipes/esp32-tinkr-led-deploy.yaml
      summary: "Flash MicroPython to a fresh ESP32 and deploy the tinkr-led example"
